#define Reciprocal(value) (1.0 / (value))

#define Min(a, b) ((a) <= (b) ? (a) : (b))
#define Max(a, b) ((a) >= (b) ? (a) : (b))

#define Clamp(value, min, max) Max(Min(value, max), min)

#define PI 3.1415927410125732421875f
